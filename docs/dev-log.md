# 开发日志 · 最终版

> 项目：互动随笔网页《杂文随笔》
> 完成：2026-08-01
> 目录：`interactive-essay/` · 站点：https://providence120.github.io/essay/

---

## 一、最终形态

单页 H5：**封面（缓冲/开启）→ 音乐响起 → 随笔流动展开**，可直接分享链接到微信/抖音聊天。

- 技术：原生 HTML/CSS/JS，**零依赖、零外部插件**
- 字体：霞鹜文楷（OFL 开源可商用），按本篇随笔子集化为 2 个 woff2（约 255KB）
- 托管：GitHub Pages，资源带自动版本号，微信不缓存旧版

## 二、最终功能

| 模块 | 效果 |
|---|---|
| 封面 | 金色粒子流 + **5 颗呼吸五角星**（均匀分布、不越界标题区、点击收缩-回弹并迸发金色光粒）+ 每 2.3 秒一道流星；整层与文字同步淡入；**仅"轻触 开启"按钮进入** |
| 音乐 | 王极《again》内置；右上角三根音柱随播放跳动（可关闭）；歌词轮播显示 |
| 随笔 | 流动式段落（滚动渐入、两格间距）、金色标题、歌词插页、落款 |
| 沉浸 | 全局粒子（星尘 + 柔光晕）、阅读进度条、移动端优先、`prefers-reduced-motion` 适配 |
| 多篇 | `js/essays.js` 每篇一个对象，`?id=篇id` 独立链接互不覆盖；多篇时自动显示目录页 |

## 三、工程要点

- **配置驱动**：换文 / 换歌 / 换主题只需改 `js/essays.js`，不动引擎。
- **字体子集化**：`scripts/build-fonts.js`（基于 `subset-font`），换文后重跑即可。
- **一键发布**：`deploy.bat` 双击完成——自动更新资源版本号（防缓存）→ `git add/commit/push`。
- **缓存策略**：css/js 引用带 `?v=` 随机版本号，每次发布自动更换，微信等浏览器不再卡旧版。

## 四、使用

```bash
# 本地预览
cd interactive-essay && npx http-server -c-1 -p 8080

# 发布（需本机代理 127.0.0.1:7897 开启）
双击 deploy.bat
```

每篇随笔分享地址：`https://providence120.github.io/essay/?id=篇id`（第一篇 `?id=shoubi`）。

## 五、已知限制

1. 音乐为商业版权歌曲，公网分享请自行确认合规。
2. 抖音私信默认拦截外链：先在浏览器打开，再"复制链接"发送。
3. iOS 静音键开启时 `<audio>` 可能无声（平台限制）。
4. 歌词未配时间轴，当前按节奏轮播；可在 `essays.js` 中为 `lyrics` 补 `time`（秒）实现精确同步。

## 六、更新记录

- 2026-08-01：裁掉《again》开头约 3.75 秒静音/极弱前奏（MPEG 帧级裁剪，保持原音质）；页面加载即预加载音频，进入瞬间出声。
- 2026-08-01：底部歌词改为"彩蛋"——仅用户手动开关音乐后才轮播显示。
- 2026-08-01：新增 **Cloudflare Pages 镜像站** `https://essay-mirror.pages.dev/`（免费、长期有效），与 GitHub Pages 互为备份，微信打不开时换用。
- 2026-08-01：**新增随笔插图功能**：`essays.js` 的小节支持 `image: {src, alt, caption}`，渲染在该小节段落后（圆角/描边/阴影/随滚动渐入）。当前在"倒数第三段"（换个角度去看…）后插入 `assets/images/photo-1.jpg`（原图 3215x1534，压缩至宽 1280、43KB）。
- 2026-08-06：**新增第二篇随笔《夜》**（docx 导入，14 段原文保留；配乐《是否》`assets/audio/shifou.mp3`，无前奏静音无需裁剪；歌词彩蛋"是否要留下遗憾…"）。
- 2026-08-06：**新流程：封面 → 选择页 → 随笔**——`轻触开启`后进入交互式选择页（金色卡片、错落入场），点选一篇进入；直链 `?id=xxx` 仍直进随笔并自动播放音乐。
- 2026-08-06：**采纳 GSAP**（官方 skill 评估后本地打包 `js/vendor/gsap.min.js` 71KB，不走 CDN）：用于选择页卡片错落入场与页面切换过渡，零外部依赖可靠性不变。
- 2026-08-06：音乐引擎保持 `<audio>`（回溯后不变）；`Music.reset()` 支持切换随笔时重置；空歌手标签处理。
- 2026-08-06：**选择页细节打磨**——封面副标"一篇一篇，慢慢写"→"未来可期"、按钮"看看"→"轻触 开启"（固定文案，增删随笔不变）；左上角加圆润**返回箭头**（回封面）；删掉"选择一篇，慢慢读"；《夜》简介改为 **Nightly Voyage**；卡片右侧进入箭头改为圆润圆形（与返回箭头同款）。去掉《夜》开头被误识别的"夜"段落。
